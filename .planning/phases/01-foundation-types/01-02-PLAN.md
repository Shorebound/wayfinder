---
phase: 01-foundation-types
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - engine/wayfinder/src/app/IApplicationState.h
  - engine/wayfinder/src/app/IOverlay.h
  - engine/wayfinder/src/app/AppSubsystem.h
  - engine/wayfinder/src/app/StateSubsystem.h
  - engine/wayfinder/src/plugins/IPlugin.h
  - engine/wayfinder/src/plugins/IStateUI.h
  - engine/wayfinder/src/plugins/ServiceProvider.h
  - engine/wayfinder/src/gameplay/Capability.h
  - engine/wayfinder/CMakeLists.txt
  - tests/app/ApplicationStateTests.cpp
  - tests/app/SubsystemTests.cpp
  - tests/plugins/ServiceProviderTests.cpp
  - tests/plugins/PluginInterfaceTests.cpp
  - tests/gameplay/TagTests.cpp
  - tests/CMakeLists.txt
autonomous: true
requirements: [STATE-01, PLUG-01, OVER-01, UI-01, SUB-01, SIM-02, SIM-04, CAP-01]

must_haves:
  truths:
    - "IApplicationState can be derived; concrete mock compiles with OnEnter/OnExit returning Result<void> and void per-frame methods"
    - "IOverlay can be derived with attach/detach/update/render/event lifecycle"
    - "IPlugin can be derived with Build(AppBuilder&) taking forward-declared AppBuilder"
    - "IStateUI can be derived with lifecycle methods mirroring IOverlay"
    - "AppSubsystem and StateSubsystem are accepted by SubsystemCollection<> and initialise/shutdown correctly"
    - "ServiceProvider concept constrains StandaloneServiceProvider at compile time and rejects non-conforming types"
    - "StandaloneServiceProvider Register/Get/TryGet provide type-safe service access"
    - "Capability tag constants declared as NativeTag objects (Simulation, Rendering, Presentation, Editing)"
    - "CapabilitySet is a type alias for TagContainer"
  artifacts:
    - path: "engine/wayfinder/src/app/IApplicationState.h"
      provides: "V2 application state interface with Result<void> lifecycle"
      contains: "class IApplicationState"
    - path: "engine/wayfinder/src/app/IOverlay.h"
      provides: "V2 overlay interface with full lifecycle"
      contains: "class IOverlay"
    - path: "engine/wayfinder/src/app/AppSubsystem.h"
      provides: "Application-scoped subsystem base"
      contains: "class AppSubsystem"
    - path: "engine/wayfinder/src/app/StateSubsystem.h"
      provides: "State-scoped subsystem base"
      contains: "class StateSubsystem"
    - path: "engine/wayfinder/src/plugins/IPlugin.h"
      provides: "V2 plugin interface with Build-only pattern"
      contains: "class IPlugin"
    - path: "engine/wayfinder/src/plugins/IStateUI.h"
      provides: "V2 per-state UI interface"
      contains: "class IStateUI"
    - path: "engine/wayfinder/src/plugins/ServiceProvider.h"
      provides: "ServiceProvider concept and StandaloneServiceProvider"
      contains: "concept ServiceProvider"
    - path: "engine/wayfinder/src/gameplay/Capability.h"
      provides: "Capability tag constants as NativeTag + CapabilitySet alias"
      contains: "namespace Wayfinder::Capability"
  key_links:
    - from: "engine/wayfinder/src/app/IApplicationState.h"
      to: "core/Result.h"
      via: "Result<void> return type on OnEnter/OnExit"
      pattern: "Result<void>"
    - from: "engine/wayfinder/src/plugins/ServiceProvider.h"
      to: "StandaloneServiceProvider"
      via: "static_assert(ServiceProvider<StandaloneServiceProvider>)"
      pattern: "static_assert.*ServiceProvider"
    - from: "engine/wayfinder/src/gameplay/Capability.h"
      to: "gameplay/NativeTag.h"
      via: "NativeTag for capability constants"
      pattern: "NativeTag"
---

<objective>
Create all v2 vocabulary type definitions: interfaces (IApplicationState, IOverlay, IPlugin, IStateUI), base types (AppSubsystem, StateSubsystem), ServiceProvider concept with StandaloneServiceProvider, capability tag constants using NativeTag, and CapabilitySet alias.

Purpose: Establishes the type vocabulary that every subsequent phase builds on. These are the contracts that subsystem infrastructure, plugin composition, and orchestration layers will program against.
Output: 8 new engine headers, updated CMakeLists.txt, 3 new test files, 2 extended test files.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-types/01-CONTEXT.md
@.planning/phases/01-foundation-types/01-RESEARCH.md
@.planning/phases/01-foundation-types/01-01-SUMMARY.md
@docs/plans/application_architecture_v2.md
@docs/plans/game_framework.md

<interfaces>
<!-- Key types the executor needs. Uses NEW names from Plan 01 rename. -->

From engine/wayfinder/src/core/Result.h:
```cpp
namespace Wayfinder {
    template<typename TType, typename TError = Error>
    using Result = std::expected<TType, TError>;
    [[nodiscard]] inline std::unexpected<Error> MakeError(std::string_view message);
}
```

From engine/wayfinder/src/gameplay/Tag.h (RENAMED in Plan 01):
```cpp
namespace Wayfinder {
    struct WAYFINDER_API Tag {
        static Tag None();
        bool operator==(const Tag& other) const;
        std::string_view GetName() const;
        bool IsValid() const;
        // Private constructors; TagRegistry is friend
    };

    struct WAYFINDER_API TagContainer {
        bool HasExact(const Tag& tag) const;
        bool HasTag(const Tag& tag) const;
        bool HasAny(const TagContainer& other) const;
        bool HasAll(const TagContainer& other) const;
        void AddTag(const Tag& tag);
        void RemoveTag(const Tag& tag);
        void AddTags(const TagContainer& other);
    };
}
```

From engine/wayfinder/src/gameplay/NativeTag.h (RENAMED in Plan 01):
```cpp
namespace Wayfinder {
    class WAYFINDER_API NativeTag {
    public:
        NativeTag(const char* name, const char* comment = "");
        operator Tag() const;  // Resolves after RegisterAll
        static void RegisterAll(TagRegistry& registry);
    };
}
```

From engine/wayfinder/src/app/Subsystem.h:
```cpp
namespace Wayfinder {
    class WAYFINDER_API Subsystem {
    public:
        virtual ~Subsystem() = default;
        virtual void Initialise() {}
        virtual void Shutdown() {}
        virtual bool ShouldCreate() const { return true; }
    };
    class WAYFINDER_API GameSubsystem : public Subsystem {};

    template<typename TBase>
    class SubsystemCollection { /* Register<T>, Get<T>, Initialise, Shutdown */ };
}
```

From engine/wayfinder/src/core/events/EventQueue.h:
```cpp
namespace Wayfinder {
    class EventQueue { /* Push<T>, Drain<T>, double-buffered */ };
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create v2 type headers and update engine CMakeLists.txt</name>
  <files>
    engine/wayfinder/src/app/IApplicationState.h
    engine/wayfinder/src/app/IOverlay.h
    engine/wayfinder/src/app/AppSubsystem.h
    engine/wayfinder/src/app/StateSubsystem.h
    engine/wayfinder/src/plugins/IPlugin.h
    engine/wayfinder/src/plugins/IStateUI.h
    engine/wayfinder/src/plugins/ServiceProvider.h
    engine/wayfinder/src/gameplay/Capability.h
    engine/wayfinder/CMakeLists.txt
  </files>
  <action>
    Create 8 new headers and update the engine CMakeLists.txt source list.

    **IApplicationState.h** (per D-01, D-02, D-03, D-04):
    - `#pragma once`, include `core/Result.h`, forward-declare `class EngineContext` and `class EventQueue` (use `class` for both - EngineContext will become a class in Phase 2)
    - Pure virtual abstract base class `IApplicationState` in `namespace Wayfinder`
    - `virtual ~IApplicationState() = default;`
    - Failable lifecycle: `virtual auto OnEnter(EngineContext& context) -> Result<void> = 0;` and `virtual auto OnExit(EngineContext& context) -> Result<void> = 0;`
    - Non-failable per-frame (virtual with empty default bodies):
      `virtual void OnSuspend(EngineContext& context) {}`
      `virtual void OnResume(EngineContext& context) {}`
      `virtual void OnUpdate(EngineContext& context, float deltaTime) {}`
      `virtual void OnRender(EngineContext& context) {}`
      `virtual void OnEvent(EngineContext& context, EventQueue& events) {}`
    - Identity: `[[nodiscard]] virtual auto GetName() const -> std::string_view = 0;`
    - No WAYFINDER_API (pure virtual, header-only)

    **IOverlay.h** (per D-02, D-03):
    - Same lifecycle pattern. Forward-declare EngineContext and EventQueue.
    - Pure virtual: `OnAttach(EngineContext&) -> Result<void>`, `OnDetach(EngineContext&) -> Result<void>`
    - Default empty: `OnUpdate(EngineContext&, float)`, `OnRender(EngineContext&)`, `OnEvent(EngineContext&, EventQueue&)`
    - Identity: `[[nodiscard]] virtual auto GetName() const -> std::string_view = 0;`

    **IPlugin.h** (per D-02, D-13):
    - In `namespace Wayfinder` (NOT `Wayfinder::Plugins` - v2 is top-level)
    - Forward-declare `class AppBuilder` (Phase 3 delivers the real class)
    - `virtual void Build(AppBuilder& builder) = 0;`
    - `virtual ~IPlugin() = default;`
    - NEW file alongside existing Plugin.h (v1 stays until Phase 7)

    **IStateUI.h** (per D-02, D-03):
    - Same lifecycle as IOverlay: attach/detach/suspend/resume/update/render/event
    - Pure virtual: `OnAttach(EngineContext&) -> Result<void>`, `OnDetach(EngineContext&) -> Result<void>`
    - Default empty: `OnSuspend(EngineContext&)`, `OnResume(EngineContext&)`, `OnUpdate(EngineContext&, float)`, `OnRender(EngineContext&)`, `OnEvent(EngineContext&, EventQueue&)`
    - Identity: `[[nodiscard]] virtual auto GetName() const -> std::string_view = 0;`

    **AppSubsystem.h** (per D-12):
    - `#pragma once`, include `Subsystem.h` and `wayfinder_exports.h`
    - `class WAYFINDER_API AppSubsystem : public Subsystem {};`
    - Doc: "Subsystem whose lifetime is tied to the Application."

    **StateSubsystem.h** (per D-12):
    - `#pragma once`, include `Subsystem.h` and `wayfinder_exports.h`
    - `class WAYFINDER_API StateSubsystem : public Subsystem {};`
    - Doc: "Subsystem whose lifetime is tied to the current ApplicationState."

    **ServiceProvider.h** (per D-05, D-06, D-07):
    - `template<typename T> concept ServiceProvider` checking `Get<int>()` returns `int&` and `TryGet<int>()` returns `int*`
    - `StandaloneServiceProvider` class with Register/Get/TryGet using `std::type_index` + `void*` map
    - `static_assert(ServiceProvider<StandaloneServiceProvider>);`
    - Include `<cassert>`, `<typeindex>`, `<unordered_map>`

    **Capability.h** (per D-16, using NativeTag from Plan 01):
    - Include `gameplay/Tag.h` and `gameplay/NativeTag.h`
    - `using CapabilitySet = TagContainer;` in `namespace Wayfinder`
    - `namespace Wayfinder::Capability` with `inline NativeTag` constants:
      `Simulation{"Capability.Simulation", "Enables simulation subsystems"}`
      `Rendering{"Capability.Rendering", "Enables rendering subsystems"}`
      `Presentation{"Capability.Presentation", "Enables presentation subsystems"}`
      `Editing{"Capability.Editing", "Enables editing subsystems"}`
    - These are `inline NativeTag` globals that self-register into the linked list.
      They become valid Tag values after `NativeTag::RegisterAll(registry)` is called during engine startup.

    **engine/wayfinder/CMakeLists.txt:**
    - Add to `# App` section (alphabetical): `src/app/AppSubsystem.h`, `src/app/IApplicationState.h`, `src/app/IOverlay.h`, `src/app/StateSubsystem.h`
    - Add to `# Gameplay` section (alphabetical): `src/gameplay/Capability.h`
    - Add to `# Plugins` section (alphabetical): `src/plugins/IPlugin.h`, `src/plugins/IStateUI.h`, `src/plugins/ServiceProvider.h`
  </action>
  <verify>
    <automated>cmake --build --preset debug --target wayfinder 2>&1 | Select-String -Pattern "error|warning" -Context 0,2</automated>
  </verify>
  <done>All 8 new headers created with correct interfaces. Engine library compiles clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create and extend tests for v2 vocabulary types</name>
  <files>
    tests/app/ApplicationStateTests.cpp
    tests/app/SubsystemTests.cpp
    tests/plugins/ServiceProviderTests.cpp
    tests/plugins/PluginInterfaceTests.cpp
    tests/gameplay/TagTests.cpp
    tests/CMakeLists.txt
  </files>
  <behavior>
    - IApplicationState: Mock derives and implements all pure virtuals; OnEnter/OnExit return success and error
    - IOverlay: Mock derives and implements OnAttach/OnDetach lifecycle
    - IPlugin: Mock derives and implements Build (forward-declared AppBuilder)
    - IStateUI: Mock derives and implements lifecycle
    - AppSubsystem: Works in SubsystemCollection<AppSubsystem>
    - StateSubsystem: Works in SubsystemCollection<StateSubsystem>
    - ServiceProvider concept: static_assert satisfied/rejected
    - StandaloneServiceProvider: Register/Get/TryGet functional
    - Capability tags: valid after registry registration, CapabilitySet usable as TagContainer
  </behavior>
  <action>
    **tests/app/ApplicationStateTests.cpp** (NEW):
    - Include `app/IApplicationState.h`, `app/IOverlay.h`, `app/EngineContext.h`, `core/events/EventQueue.h`, `<doctest/doctest.h>`
    - MockApplicationState: tracks calls, OnEnter/OnExit return `Result<void>{}`, GetName returns "MockState"
    - TEST_CASE "IApplicationState mock compiles and implements lifecycle"
    - TEST_CASE "IApplicationState OnEnter can return error" (mock returns MakeError)
    - TEST_CASE "IOverlay mock compiles and implements lifecycle"

    **tests/plugins/PluginInterfaceTests.cpp** (NEW):
    - Include `plugins/IPlugin.h`, `plugins/IStateUI.h`, `app/EngineContext.h`, `core/events/EventQueue.h`
    - MockPlugin: overrides Build (can't call it without AppBuilder, just verify compilation)
    - TEST_CASE "IPlugin mock compiles and instantiates"
    - MockStateUI: full lifecycle mock
    - TEST_CASE "IStateUI mock compiles and implements lifecycle"

    **tests/plugins/ServiceProviderTests.cpp** (NEW):
    - Include `plugins/ServiceProvider.h`, `<doctest/doctest.h>`
    - TEST_CASE "ServiceProvider concept accepts StandaloneServiceProvider": static_assert
    - TEST_CASE "ServiceProvider concept rejects non-conforming type": static_assert(not ServiceProvider<int>)
    - TEST_CASE "StandaloneServiceProvider Register and Get": register int, Get returns reference
    - TEST_CASE "StandaloneServiceProvider TryGet for unregistered": returns nullptr
    - TEST_CASE "StandaloneServiceProvider multiple services": register int and float independently

    **tests/app/SubsystemTests.cpp** (EXTEND):
    - Add test classes TestAppSubsystem and TestStateSubsystem with lifecycle logging
    - TEST_CASE "AppSubsystem registers and initialises in SubsystemCollection"
    - TEST_CASE "StateSubsystem registers and initialises in SubsystemCollection"

    **tests/gameplay/TagTests.cpp** (EXTEND - was renamed from GameplayTagTests.cpp in Plan 01):
    - Add test cases for Capability and CapabilitySet at the end
    - Include `gameplay/Capability.h`, `gameplay/NativeTag.h`, `gameplay/TagRegistry.h`
    - TEST_CASE "Capability tags are valid after NativeTag::RegisterAll": create TagRegistry, call NativeTag::RegisterAll, check each Capability constant IsValid() and has correct name
    - TEST_CASE "CapabilitySet is usable as TagContainer": create CapabilitySet, AddTag, HasExact
    - TEST_CASE "AddTags merges containers": two containers, merge, verify
    - TEST_CASE "AddTags deduplicates overlapping tags": merge with overlap, check Size()

    **tests/CMakeLists.txt:**
    - Add to `wayfinder_core_tests`: `app/ApplicationStateTests.cpp`, `plugins/ServiceProviderTests.cpp`, `plugins/PluginInterfaceTests.cpp`
  </action>
  <verify>
    <automated>cmake --build --preset debug --target wayfinder_core_tests; ctest --preset test -R wayfinder_core_tests --output-on-failure</automated>
  </verify>
  <done>All tests pass. V2 interfaces verified via mocks. Subsystem scopes work. ServiceProvider concept proven. Capability tags resolve correctly via NativeTag::RegisterAll.</done>
</task>

</tasks>

<verification>
1. Engine library compiles: `cmake --build --preset debug --target wayfinder`
2. Core tests pass: `cmake --build --preset debug --target wayfinder_core_tests && ctest --preset test -R wayfinder_core_tests`
3. Full suite unbroken: `ctest --preset test`
</verification>

<success_criteria>
- 8 new engine headers with correct interfaces matching CONTEXT.md decisions
- Capability.h uses NativeTag (not FromName) for tag constants
- CapabilitySet is TagContainer alias
- All new and extended tests pass
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-types/01-02-SUMMARY.md`
</output>
