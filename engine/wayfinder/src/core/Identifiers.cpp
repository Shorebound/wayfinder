// Identifiers.cpp — intentionally minimal.
// Uuid, TypedId, SceneObjectId, and AssetId are now fully header-defined.
// This file exists only to keep the build system happy if it expects a translation unit.

#include "Identifiers.h"
