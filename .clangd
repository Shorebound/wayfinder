# compile_commands.json is generated by the dev preset (Ninja + MSVC).
# Requires: cmake --preset dev  (from a VS Developer Shell)
# Clang alternative: change to build/clang
CompileFlags:
  CompilationDatabase: build/dev
